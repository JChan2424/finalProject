const Round = require('../models/Round.js')

const getByName = (req,res)=>{
    
}

const getByCombo = (req,res)=>{ // May not need this one
    
}

const postRound = (req,res)=>{
    
}

module.exports = {
    getByName,
    getByCombo,
    postRound
}